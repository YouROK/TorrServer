# Go toolchain versions — single source for CI, GoReleaser, and local release builds.
# server/go.mod "go" directive should stay aligned with GO_ANDROID_VERSION (module minimum).
GO_VERSION=1.26.4
GO_ANDROID_VERSION=1.25.7
